# Pambala API

## Regra do projeto

**Separar as alterações de código das alterações de documentação em commits distintos.** Sempre que mudar código/estrutura/endpoints/variáveis de ambiente, atualize o `README.md` (e docs relacionados, como `docs/` e `receipt_agent/README.md`) no mesmo fluxo de trabalho e commite a documentação separadamente.